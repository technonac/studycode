# 元组 tuple 不可修改的序列
t = (1, 2, 3)
print(t)

# 空元组
e_t = ()

# 只包含1个值的元组,必须在后面加上逗号
tup1 = (42,)

# 将列表转为元组
tuple_from_list = tuple(['a', 'b', 'c'])
print(tuple_from_list)
tuple_from_string = tuple('abc')
print(tuple_from_string)

